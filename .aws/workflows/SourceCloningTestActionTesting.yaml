Name: SourceCloningTestActionTesting
SchemaVersion: 1.0

Actions:
  Test:
    Identifier: aws/managed-test-gamma@v1
    Inputs:
      Sources:
        - WorkflowSource
    Configuration:
      Steps:
        - Run: echo "hello world" > test.txt
        - Run: cat test.txt
        - Run: git log